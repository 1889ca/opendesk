/** Contract: contracts/app-kb/rules.md */

// app-kb uses KBEntryRecord type defined in kb-api.ts (runtime type, not schema).
// No Zod schemas — the KB module on the server side owns the canonical schema.
